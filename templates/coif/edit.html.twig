{% extends 'base.html.twig' %}

{% block title %}Edit Coif{% endblock %}

{% block body %}
    <h1>Edit Coif</h1>

    {{ include('coif/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('coif_index') }}">back to list</a>

    {{ include('coif/_delete_form.html.twig') }}
{% endblock %}
